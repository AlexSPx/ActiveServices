spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/active_services
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
